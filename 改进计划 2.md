# 🚀 Sustainable Life App 优化计划 2.0

## 📋 文档信息

- **版本**: 2.0
- **日期**: 2026-03-11
- **范围**: 基于完整业务流程分析的深度优化
- **目标**: 提升用户体验、代码质量、系统稳定性
- **原则**: 
  1. 单一职责 - 一个功能一个模块
  2. 代码复用 - 优先继承现有模块
  3. 重构清理 - 废弃代码标记后删除
  4. 风格一致 - 保持现有 UI/UX 风格
  5. 最小代码 - 抽象符合"三次法则"
  6. 兼容性优先 - Settings 等配置可继承

---

## 📊 当前状态分析总结

### 已完成功能（90%）
- ✅ CoreData 模型完整（2 个实体）
- ✅ Repository 模式架构清晰
- ✅ CO2 计算引擎（US 排放因子）
- ✅ 数据验证层完善
- ✅ Dashboard/Profile/Settings 主界面
- ✅ 活动记录功能
- ✅ 成就系统（基础版）
- ✅ 通知服务（基础版）
- ✅ 单元测试（54+ 用例）

### 需要优化的痛点（基于流程分析）

#### 1. **成就系统不完善** 🔴
- ❌ 成就解锁后缺少弹窗动画
- ❌ 没有与 NotificationService 集成
- ❌ Dashboard 成就展示为 TODO
- ❌ 成就进度更新不及时

#### 2. **通知系统未完全实现** 🟡
- ❌ 每日提醒未在 Settings 中完全配置
- ❌ 每周总结未实现
- ❌ 连续打卡保护未实现
- ❌ 成就解锁通知未触发

#### 3. **数据导出功能简陋** 🟡
- ❌ 只有 CSV/JSON，缺少 PDF
- ❌ 没有生物识别验证
- ❌ 导出界面未实现（ExportView 为空）
- ❌ 分享功能未集成

#### 4. **Profile 功能单薄** 🟡
- ❌ 成就墙只显示 6 个，无详情
- ❌ 统计数据不够详细
- ❌ 缺少编辑功能

#### 5. **代码复用不足** 🟡
- ❌ 多个 ViewModel 重复加载数据逻辑
- ❌ 成就检查逻辑分散
- ❌ 通知调度代码重复

---

## 🎯 优化项目清单

### 优化 1: 成就系统完善与动画效果 ⭐⭐⭐⭐⭐

#### 需求描述
完善成就系统的用户体验，包括解锁动画、通知集成、进度实时更新，提升用户成就感。

#### 生成规则
- **模块位置**: 
  - `/Services/AchievementService.swift` (增强)
  - `/Views/Achievement/AchievementUnlockPopup.swift` (新增)
  - `/ViewModels/DashboardViewModel.swift` (增强)
- **依赖关系**: 继承现有 AchievementService，不重写
- **触发时机**: 活动保存后立即检查
- **展示位置**: Dashboard 弹窗 + Profile 成就墙

#### 检验标准

##### 1. 代码示例

**文件 1: AchievementService.swift (增强版)**

```swift
//
//  AchievementService.swift
//  suslife
//
//  Achievement Service - Enhanced with animations and notifications
//

import Foundation
import SwiftUI

@MainActor
final class AchievementService: ObservableObject {
    // MARK: - Published Properties
    
    @Published private(set) var achievements: [Achievement] = Achievement.allAchievements
    @Published private(set) var recentlyUnlocked: [Achievement] = []
    @Published private(set) var totalXP: Int = 0
    @Published var showUnlockPopup = false  // 新增：控制弹窗显示
    @Published var currentUnlockAchievement: Achievement?  // 新增：当前解锁的成就
    
    // MARK: - Dependencies
    
    private let repository: ActivityRepositoryProtocol
    private let notificationService: NotificationService  // 新增：通知服务
    
    // MARK: - Initialization
    
    init(
        repository: ActivityRepositoryProtocol = CoreDataActivityRepository(),
        notificationService: NotificationService = .shared  // 新增：依赖注入
    ) {
        self.repository = repository
        self.notificationService = notificationService
        loadXP()
    }
    
    // MARK: - Public Methods
    
    /// Check achievements after activity saved
    func checkAchievements() async {
        var newlyUnlocked: [Achievement] = []
        
        do {
            // Fetch all required data
            let totalActivities = try await repository.fetchTotalActivities()
            let totalCO2Saved = try await repository.calculateTotalCO2Saved()
            let streak = try await repository.fetchActivityCount(for: .last7Days)
            let monthlyTrend = try await repository.fetchMonthlyTrend()
            
            // Check each achievement
            for index in achievements.indices {
                let achievement = achievements[index]
                if achievement.isUnlocked { continue }
                
                let shouldUnlock = checkRequirement(
                    achievement.requirement,
                    totalActivities: totalActivities,
                    totalCO2Saved: totalCO2Saved,
                    streakDays: streak,
                    monthlyTrend: monthlyTrend
                )
                
                if shouldUnlock {
                    unlockAchievement(at: index)
                    newlyUnlocked.append(achievements[index])
                    totalXP += achievements[index].xpReward
                    
                    // 新增：触发通知和弹窗
                    await handleAchievementUnlock(achievements[index])
                } else {
                    updateProgress(
                        at: index,
                        totalActivities: totalActivities,
                        totalCO2Saved: totalCO2Saved,
                        streakDays: streak,
                        monthlyTrend: monthlyTrend
                    )
                }
            }
            
            if !newlyUnlocked.isEmpty {
                recentlyUnlocked = newlyUnlocked
                saveXP()
            }
        } catch {
            print("Error checking achievements: \(error)")
        }
    }
    
    /// Handle achievement unlock with notification and popup
    private func handleAchievementUnlock(_ achievement: Achievement) async {
        // 1. Show popup
        currentUnlockAchievement = achievement
        showUnlockPopup = true
        
        // 2. Send notification (if authorized)
        if notificationService.isAuthorized {
            await notificationService.showAchievementUnlockedNotification(
                title: "🎉 Achievement Unlocked!",
                body: achievement.title,
                iconName: achievement.iconName
            )
        }
        
        // 3. Haptic feedback
        let impactGenerator = UIImpactFeedbackGenerator(style: .medium)
        impactGenerator.impactOccurred()
        
        // 4. Auto-hide popup after 3 seconds
        try? await Task.sleep(nanoseconds: 3_000_000_000)
        await MainActor.run {
            showUnlockPopup = false
            currentUnlockAchievement = nil
        }
    }
    
    // MARK: - Private Methods
    
    private func checkRequirement(
        _ requirement: AchievementRequirement,
        totalActivities: Int,
        totalCO2Saved: Double,
        streakDays: Int,
        monthlyTrend: [DailyTotal]
    ) -> Bool {
        switch requirement {
        case .totalActivities(let count):
            return totalActivities >= count
        case .streakDays(let days):
            return streakDays >= days
        case .totalCO2Saved(let kg):
            return totalCO2Saved >= kg
        case .weeklyActivities(_, let count):
            return weeklyActivitiesCount(monthlyTrend) >= count
        case .categoryMilestone:
            return false
        case .consecutiveDays(let days):
            return calculateConsecutiveDays(monthlyTrend) >= days
        }
    }
    
    // ... 其他方法保持不变
}
```

**文件 2: AchievementUnlockPopup.swift (新增)**

```swift
//
//  AchievementUnlockPopup.swift
//  suslife
//
//  Achievement Unlock Popup - Full-screen celebration animation
//

import SwiftUI

struct AchievementUnlockPopup: View {
    let achievement: Achievement
    @Binding var isPresented: Bool
    
    @State private var scale: CGFloat = 0.5
    @State private var opacity: Double = 0
    @State private var rotation: Double = -10
    
    var body: some View {
        ZStack {
            // Background overlay
            Color.black.opacity(0.7)
                .ignoresSafeArea()
                .onTapGesture {
                    withAnimation {
                        isPresented = false
                    }
                }
            
            // Achievement card
            VStack(spacing: 24) {
                // Icon
                ZStack {
                    Circle()
                        .fill(AppColors.primary.opacity(0.2))
                        .frame(width: 100, height: 100)
                    
                    Image(systemName: achievement.iconName)
                        .font(.system(size: 50))
                        .foregroundColor(AppColors.primary)
                        .symbolEffect(.bounce, options: .repeating)
                }
                
                // Title
                Text("🎉 Achievement Unlocked!")
                    .font(Fonts.title2)
                    .fontWeight(.bold)
                    .foregroundColor(.white)
                
                // Achievement name
                Text(achievement.title)
                    .font(Fonts.headline)
                    .fontWeight(.semibold)
                    .foregroundColor(.white)
                
                // Description
                Text(achievement.description)
                    .font(Fonts.body)
                    .foregroundColor(.white.opacity(0.8))
                    .multilineTextAlignment(.center)
                    .padding(.horizontal)
                
                // XP reward
                HStack(spacing: 8) {
                    Image(systemName: "star.fill")
                        .foregroundColor(.yellow)
                    Text("+\(achievement.xpReward) XP")
                        .font(Fonts.title3)
                        .fontWeight(.bold)
                        .foregroundColor(.yellow)
                }
                .padding(.horizontal, 20)
                .padding(.vertical, 10)
                .background(Color.white.opacity(0.2))
                .cornerRadius(20)
            }
            .padding(40)
            .background(Color(.systemBackground))
            .cornerRadius(24)
            .shadow(color: .black.opacity(0.3), radius: 20, x: 0, y: 10)
            .scaleEffect(scale)
            .opacity(opacity)
            .rotationEffect(.degrees(rotation))
        }
        .onAppear {
            playAnimation()
        }
    }
    
    private func playAnimation() {
        withAnimation(.spring(response: 0.5, dampingFraction: 0.7)) {
            scale = 1.0
            opacity = 1.0
            rotation = 0
        }
    }
}

#Preview {
    AchievementUnlockPopup(
        achievement: Achievement.allAchievements[0],
        isPresented: .constant(true)
    )
}
```

**文件 3: DashboardView.swift (增强)**

```swift
// 在 DashboardView 中添加成就弹窗
struct DashboardView: View {
    @StateObject private var viewModel = DashboardViewModel()
    @StateObject private var achievementService = AchievementService()
    @State private var showingLogSheet = false
    @State private var selectedCategory: String? = nil
    
    var body: some View {
        NavigationView {
            ZStack {
                ScrollView {
                    // ... 现有内容
                }
                
                // 成就解锁弹窗
                if achievementService.showUnlockPopup,
                   let achievement = achievementService.currentUnlockAchievement {
                    AchievementUnlockPopup(
                        achievement: achievement,
                        isPresented: $achievementService.showUnlockPopup
                    )
                    .transition(.opacity)
                    .zIndex(100)
                }
            }
            .navigationTitle("Sustainable Life")
            .task {
                await viewModel.loadData()
                await achievementService.checkAchievements()
            }
        }
    }
}
```

##### 2. 测试用例

```swift
//
//  AchievementServiceTests.swift
//  suslifeTests
//
//  Enhanced achievement tests
//

import XCTest
@testable import suslife

@MainActor
final class AchievementServiceTests: XCTestCase {
    
    var mockRepository: MockActivityRepository!
    var mockNotificationService: MockNotificationService!
    var service: AchievementService!
    
    override func setUp() async throws {
        mockRepository = MockActivityRepository()
        mockNotificationService = MockNotificationService()
        service = AchievementService(
            repository: mockRepository,
            notificationService: mockNotificationService
        )
    }
    
    /// Test first activity achievement unlocks
    func testFirstActivityAchievement() async throws {
        // Setup: Mock 1 total activity
        mockRepository.totalActivities = 1
        
        // Act
        await service.checkAchievements()
        
        // Assert
        let firstStepAchievement = service.achievements.first { $0.id == "first_step" }
        XCTAssertTrue(firstStepAchievement?.isUnlocked ?? false)
        XCTAssertTrue(mockNotificationService.notificationSent)
        XCTAssertEqual(mockNotificationService.lastTitle, "🎉 Achievement Unlocked!")
    }
    
    /// Test achievement popup is triggered
    func testAchievementPopupTriggered() async throws {
        // Setup
        mockRepository.totalActivities = 10
        
        // Act
        await service.checkAchievements()
        
        // Assert
        XCTAssertTrue(service.showUnlockPopup)
        XCTAssertNotNil(service.currentUnlockAchievement)
        XCTAssertEqual(service.currentUnlockAchievement?.id, "ten_activities")
    }
    
    /// Test XP is accumulated correctly
    func testXPAccumulation() async throws {
        // Setup
        mockRepository.totalActivities = 1
        
        // Act
        await service.checkAchievements()
        let initialXP = service.totalXP
        
        // Setup second achievement
        mockRepository.totalActivities = 10
        
        // Act
        await service.checkAchievements()
        
        // Assert
        XCTAssertGreaterThan(service.totalXP, initialXP)
        XCTAssertEqual(service.totalXP, 10 + 50) // first_step + ten_activities
    }
    
    /// Test achievement progress updates
    func testProgressUpdates() async throws {
        // Setup: 5 activities (halfway to 10)
        mockRepository.totalActivities = 5
        
        // Act
        await service.checkAchievements()
        
        // Assert
        let tenActivitiesAchievement = service.achievements.first { $0.id == "ten_activities" }
        XCTAssertEqual(tenActivitiesAchievement?.progress, 0.5)
        XCTAssertFalse(tenActivitiesAchievement?.isUnlocked ?? true)
    }
}

// Mock Notification Service
@MainActor
final class MockNotificationService: NotificationService {
    var notificationSent = false
    var lastTitle: String?
    var lastBody: String?
    
    override func showAchievementUnlockedNotification(
        title: String,
        body: String,
        iconName: String
    ) async {
        notificationSent = true
        lastTitle = title
        lastBody = body
    }
}
```

##### 3. 验收标准

- ✅ 记录活动后立即检查成就
- ✅ 解锁成就时显示全屏弹窗动画
- ✅ 弹窗包含成就图标、名称、描述、XP 奖励
- ✅ 弹窗有弹跳动画效果（scale + rotation）
- ✅ 发送本地通知（如果已授权）
- ✅ 触发触觉反馈（Haptic Feedback）
- ✅ 3 秒后自动关闭弹窗
- ✅ 成就数据持久化（UserDefaults）
- ✅ Dashboard 和 Profile 同步更新
- ✅ 测试覆盖率 > 90%

---

### 优化 2: 通知系统完善 ⭐⭐⭐⭐

#### 需求描述
完善通知系统，实现每日提醒、每周总结、连续打卡保护、成就解锁通知，提升用户留存率。

#### 生成规则
- **模块位置**: `/Services/NotificationService.swift` (增强)
- **继承关系**: 扩展现有 NotificationService，不破坏原有接口
- **配置入口**: SettingsView → Notifications Section
- **触发机制**: 本地通知，无需网络

#### 检验标准

##### 1. 代码示例

**文件：NotificationService.swift (增强版)**

```swift
//
//  NotificationService.swift
//  suslife
//
//  Notification Service - Enhanced with all reminder types
//

import Foundation
import UserNotifications
import UIKit

@MainActor
final class NotificationService: ObservableObject {
    // MARK: - Singleton
    
    static let shared = NotificationService()
    
    // MARK: - Published Properties
    
    @Published var isAuthorized = false
    @Published var authorizationStatus: UNAuthorizationStatus = .notDetermined
    
    // MARK: - Private Properties
    
    private let notificationCenter = UNUserNotificationCenter.current()
    
    // MARK: - Initialization
    
    init() {
        Task {
            await checkAuthorizationStatus()
        }
    }
    
    // MARK: - Authorization
    
    func checkAuthorizationStatus() async {
        let settings = await notificationCenter.notificationSettings()
        authorizationStatus = settings.authorizationStatus
        isAuthorized = settings.authorizationStatus == .authorized
    }
    
    func requestAuthorization() async -> Bool {
        do {
            let granted = try await notificationCenter.requestAuthorization(
                options: [.alert, .badge, .sound]
            )
            await checkAuthorizationStatus()
            return granted
        } catch {
            print("Notification authorization error: \(error)")
            return false
        }
    }
    
    // MARK: - Daily Reminder
    
    func scheduleDailyReminder(at hour: Int, minute: Int) async {
        guard isAuthorized else { return }
        
        await cancelNotification(withIdentifier: "daily_reminder")
        
        let content = UNMutableNotificationContent()
        content.title = "🌱 Daily Eco Reminder"
        content.body = "Don't forget to log your sustainable activities today!"
        content.sound = .default
        content.badge = 1
        
        var dateComponents = DateComponents()
        dateComponents.hour = hour
        dateComponents.minute = minute
        
        let trigger = UNCalendarNotificationTrigger(
            dateMatching: dateComponents,
            repeats: true
        )
        
        let request = UNNotificationRequest(
            identifier: "daily_reminder",
            content: content,
            trigger: trigger
        )
        
        do {
            try await notificationCenter.add(request)
            saveReminderTime(hour: hour, minute: minute)
        } catch {
            print("Failed to schedule daily reminder: \(error)")
        }
    }
    
    // MARK: - Weekly Summary (新增)
    
    func scheduleWeeklySummary() async {
        guard isAuthorized else { return }
        
        await cancelNotification(withIdentifier: "weekly_summary")
        
        let content = UNMutableNotificationContent()
        content.title = "📊 Weekly Eco Summary"
        content.body = "Check out your weekly sustainability progress and see how you're making a difference!"
        content.sound = .default
        content.categoryIdentifier = "weekly_summary_action"
        
        var dateComponents = DateComponents()
        dateComponents.weekday = 1  // Sunday
        dateComponents.hour = 10
        dateComponents.minute = 0
        
        let trigger = UNCalendarNotificationTrigger(
            dateMatching: dateComponents,
            repeats: true
        )
        
        let request = UNNotificationRequest(
            identifier: "weekly_summary",
            content: content,
            trigger: trigger
        )
        
        do {
            try await notificationCenter.add(request)
        } catch {
            print("Failed to schedule weekly summary: \(error)")
        }
    }
    
    // MARK: - Streak Protection (新增)
    
    func scheduleStreakReminder() async {
        guard isAuthorized else { return }
        
        await cancelNotification(withIdentifier: "streak_reminder")
        
        // Only schedule if user has active streak
        let hasActiveStreak = await checkActiveStreak()
        guard hasActiveStreak else { return }
        
        let content = UNMutableNotificationContent()
        content.title = "🔥 Keep Your Streak!"
        content.body = "You have an active streak. Log an activity today to keep it going!"
        content.sound = .default
        content.badge = 1
        
        var dateComponents = DateComponents()
        dateComponents.hour = 18
        dateComponents.minute = 0
        
        let trigger = UNCalendarNotificationTrigger(
            dateMatching: dateComponents,
            repeats: true
        )
        
        let request = UNNotificationRequest(
            identifier: "streak_reminder",
            content: content,
            trigger: trigger
        )
        
        do {
            try await notificationCenter.add(request)
        } catch {
            print("Failed to schedule streak reminder: \(error)")
        }
    }
    
    // MARK: - Achievement Unlocked (新增)
    
    func showAchievementUnlockedNotification(
        title: String,
        body: String,
        iconName: String
    ) async {
        guard isAuthorized else { return }
        
        let content = UNMutableNotificationContent()
        content.title = title
        content.body = body
        content.sound = .default
        content.badge = 0
        content.categoryIdentifier = "achievement_action"
        
        let request = UNNotificationRequest(
            identifier: "achievement_\(UUID().uuidString)",
            content: content,
            trigger: nil  // Immediate delivery
        )
        
        do {
            try await notificationCenter.add(request)
        } catch {
            print("Failed to show achievement notification: \(error)")
        }
    }
    
    // MARK: - Cancellation
    
    func cancelAllNotifications() async {
        notificationCenter.removeAllPendingNotificationRequests()
        notificationCenter.removeAllDeliveredNotifications()
    }
    
    func cancelDailyReminder() {
        notificationCenter.removePendingNotificationRequests(
            withIdentifiers: ["daily_reminder"]
        )
    }
    
    private func cancelNotification(withIdentifier identifier: String) {
        notificationCenter.removePendingNotificationRequests(
            withIdentifiers: [identifier]
        )
    }
    
    func clearBadge() {
        UNUserNotificationCenter.current().setBadgeCount(0) { _ in }
    }
    
    // MARK: - Helper Methods
    
    private func saveReminderTime(hour: Int, minute: Int) {
        UserDefaults.standard.set(hour, forKey: "reminder_hour")
        UserDefaults.standard.set(minute, forKey: "reminder_minute")
    }
    
    func getSavedReminderTime() -> (hour: Int, minute: Int)? {
        let hour = UserDefaults.standard.integer(forKey: "reminder_hour")
        let minute = UserDefaults.standard.integer(forKey: "reminder_minute")
        
        if hour > 0 || minute > 0 {
            return (hour, minute)
        }
        return nil
    }
    
    private func checkActiveStreak() async -> Bool {
        // TODO: Integrate with repository to check streak
        // For now, return false
        return false
    }
}

// MARK: - Notification Categories (新增)

extension NotificationService {
    func registerNotificationCategories() {
        // Weekly summary action category
        let viewAction = UNNotificationAction(
            identifier: "VIEW_WEEKLY",
            title: "View Summary",
            options: .foreground
        )
        
        let weeklyCategory = UNNotificationCategory(
            identifier: "weekly_summary_action",
            actions: [viewAction],
            intentIdentifiers: [],
            options: []
        )
        
        // Achievement action category
        let viewAchievementAction = UNNotificationAction(
            identifier: "VIEW_ACHIEVEMENT",
            title: "View Achievement",
            options: .foreground
        )
        
        let achievementCategory = UNNotificationCategory(
            identifier: "achievement_action",
            actions: [viewAchievementAction],
            intentIdentifiers: [],
            options: []
        )
        
        notificationCenter.setNotificationCategories([
            weeklyCategory,
            achievementCategory
        ])
    }
}
```

##### 2. 测试用例

```swift
//
//  NotificationServiceTests.swift
//  suslifeTests
//
//  Notification tests
//

import XCTest
@testable import suslife

@MainActor
final class NotificationServiceTests: XCTestCase {
    
    var service: NotificationService!
    
    override func setUp() async throws {
        service = NotificationService.shared
    }
    
    /// Test daily reminder scheduling
    func testScheduleDailyReminder() async throws {
        // Setup
        service.isAuthorized = true
        
        // Act
        await service.scheduleDailyReminder(at: 9, minute: 0)
        
        // Assert
        let pendingRequests = await UNUserNotificationCenter.current()
            .pendingNotificationRequests()
        
        let dailyReminder = pendingRequests.first {
            $0.identifier == "daily_reminder"
        }
        
        XCTAssertNotNil(dailyReminder)
        XCTAssertEqual(dailyReminder?.content.title, "🌱 Daily Eco Reminder")
        
        // Verify saved time
        XCTAssertEqual(UserDefaults.standard.integer(forKey: "reminder_hour"), 9)
        XCTAssertEqual(UserDefaults.standard.integer(forKey: "reminder_minute"), 0)
    }
    
    /// Test weekly summary scheduling
    func testScheduleWeeklySummary() async throws {
        // Setup
        service.isAuthorized = true
        
        // Act
        await service.scheduleWeeklySummary()
        
        // Assert
        let pendingRequests = await UNUserNotificationCenter.current()
            .pendingNotificationRequests()
        
        let weeklySummary = pendingRequests.first {
            $0.identifier == "weekly_summary"
        }
        
        XCTAssertNotNil(weeklySummary)
        XCTAssertEqual(weeklySummary?.content.title, "📊 Weekly Eco Summary")
    }
    
    /// Test achievement notification
    func testAchievementNotification() async throws {
        // Setup
        service.isAuthorized = true
        
        // Act
        await service.showAchievementUnlockedNotification(
            title: "🎉 Achievement Unlocked!",
            body: "First Step",
            iconName: "leaf.fill"
        )
        
        // Assert
        let pendingRequests = await UNUserNotificationCenter.current()
            .pendingNotificationRequests()
        
        let achievementNotification = pendingRequests.first {
            $0.content.title == "🎉 Achievement Unlocked!"
        }
        
        XCTAssertNotNil(achievementNotification)
        XCTAssertEqual(achievementNotification?.content.body, "First Step")
    }
    
    /// Test notification cancellation
    func testCancelDailyReminder() async throws {
        // Setup
        service.isAuthorized = true
        await service.scheduleDailyReminder(at: 9, minute: 0)
        
        // Act
        service.cancelDailyReminder()
        
        // Assert
        let pendingRequests = await UNUserNotificationCenter.current()
            .pendingNotificationRequests()
        
        let dailyReminder = pendingRequests.first {
            $0.identifier == "daily_reminder"
        }
        
        XCTAssertNil(dailyReminder)
    }
}
```

##### 3. 验收标准

- ✅ 每日提醒可自定义时间（Settings 配置）
- ✅ 每周总结固定周日 10:00 发送
- ✅ 连续打卡保护每天 18:00 检查（仅活跃 streak）
- ✅ 成就解锁立即发送通知
- ✅ 通知支持点击跳转（Deep Link）
- ✅ 通知分类注册（支持快捷操作）
- ✅ Badge 计数正确
- ✅ 取消功能正常工作
- ✅ 测试覆盖率 > 85%

---

### 优化 3: 数据导出功能完善 ⭐⭐⭐⭐

#### 需求描述
完善数据导出功能，支持 CSV/JSON/PDF格式，添加生物识别验证，实现系统分享集成。

#### 生成规则
- **模块位置**: 
  - `/Services/ExportService.swift` (增强)
  - `/Views/Settings/ExportView.swift` (实现)
- **依赖关系**: 继承现有 ExportService
- **安全要求**: 导出前需生物识别验证
- **分享方式**: 系统 UIActivityViewController

#### 检验标准

##### 1. 代码示例

**文件：ExportService.swift (增强版)**

```swift
//
//  ExportService.swift
//  suslife
//
//  Export Service - Enhanced with PDF and biometric auth
//

import Foundation
import UniformTypeIdentifiers
import UIKit

final class ExportService {
    // MARK: - Properties
    
    private let repository: ActivityRepositoryProtocol
    
    // MARK: - Initialization
    
    init(repository: ActivityRepositoryProtocol = CoreDataActivityRepository()) {
        self.repository = repository
    }
    
    // MARK: - CSV Export
    
    func exportToCSV() async throws -> URL {
        let activities = try await repository.fetchActivities(
            from: .distantPast,
            to: Date()
        )
        
        var csvContent = "Date,Category,Activity Type,Value,Unit,CO2 Saved (lbs),Notes\n"
        
        let dateFormatter = DateFormatter()
        dateFormatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        
        for activity in activities {
            let row = [
                dateFormatter.string(from: activity.date),
                activity.category ?? "",
                activity.activityType ?? "",
                String(format: "%.2f", activity.value),
                activity.unit ?? "",
                String(format: "%.2f", activity.co2Emission),
                activity.notes ?? ""
            ].joined(separator: ",")
            
            csvContent += row + "\n"
        }
        
        let fileName = "suslife_export_\(dateFormatter.string(from: Date())).csv"
        let tempURL = FileManager.default.temporaryDirectory
            .appendingPathComponent(fileName)
        
        try csvContent.write(to: tempURL, atomically: true, encoding: .utf8)
        
        return tempURL
    }
    
    // MARK: - JSON Export
    
    func exportToJSON() async throws -> URL {
        let activities = try await repository.fetchActivities(
            from: .distantPast,
            to: Date()
        )
        
        let exportData = activities.map { activity -> [String: Any] in
            var dict: [String: Any] = [
                "id": activity.id.uuidString,
                "date": ISO8601DateFormatter().string(from: activity.date),
                "category": activity.category ?? "",
                "activityType": activity.activityType ?? "",
                "value": activity.value,
                "unit": activity.unit ?? "",
                "co2Emission": activity.co2Emission
            ]
            
            if let notes = activity.notes {
                dict["notes"] = notes
            }
            
            return dict
        }
        
        let jsonData = try JSONSerialization.data(
            withJSONObject: exportData,
            options: .prettyPrinted
        )
        
        let dateFormatter = DateFormatter()
        dateFormatter.dateFormat = "yyyy-MM-dd_HHmmss"
        let fileName = "suslife_export_\(dateFormatter.string(from: Date())).json"
        let tempURL = FileManager.default.temporaryDirectory
            .appendingPathComponent(fileName)
        
        try jsonData.write(to: tempURL)
        
        return tempURL
    }
    
    // MARK: - PDF Export (新增)
    
    func exportToPDF() async throws -> URL {
        // Get statistics
        let totalCO2Saved = try await repository.calculateTotalCO2Saved()
        let totalActivities = try await repository.fetchTotalActivities()
        let weeklyTrend = try await repository.fetchWeeklyTrend()
        
        // Create PDF
        let pdfRenderer = UIGraphicsPDFRenderer(bounds: CGRect(x: 0, y: 0, width: 612, height: 792))
        
        let dateFormatter = DateFormatter()
        dateFormatter.dateFormat = "yyyy-MM-dd"
        let fileName = "suslife_report_\(dateFormatter.string(from: Date())).pdf"
        let tempURL = FileManager.default.temporaryDirectory.appendingPathComponent(fileName)
        
        try pdfRenderer.writePDF(to: tempURL) { context in
            // Page 1: Summary
            context.beginPage()
            
            // Title
            let title = "Sustainable Life Report"
            let titleAttributes: [NSAttributedString.Key: Any] = [
                .font: UIFont.boldSystemFont(ofSize: 24),
                .foregroundColor: UIColor.black
            ]
            let titleSize = title.size(withAttributes: titleAttributes)
            title.draw(at: CGPoint(x: 306 - titleSize.width / 2, y: 50))
            
            // Date
            let dateStr = "Generated on \(dateFormatter.string(from: Date()))"
            let dateAttributes: [NSAttributedString.Key: Any] = [
                .font: UIFont.systemFont(ofSize: 12),
                .foregroundColor: UIColor.gray
            ]
            let dateSize = dateStr.size(withAttributes: dateAttributes)
            dateStr.draw(
                at: CGPoint(x: 306 - dateSize.width / 2, y: 80),
                withAttributes: dateAttributes
            )
            
            // Statistics
            var yPosition: CGFloat = 150
            
            let stats = [
                "Total CO₂ Saved: \(String(format: "%.1f", totalCO2Saved)) lbs",
                "Total Activities: \(totalActivities)",
                "Weekly Average: \(String(format: "%.1f", weeklyTrend.reduce(0) { $0 + $1.totalCO2 } / Double(weeklyTrend.count))) lbs"
            ]
            
            for stat in stats {
                let statAttributes: [NSAttributedString.Key: Any] = [
                    .font: UIFont.systemFont(ofSize: 16),
                    .foregroundColor: UIColor.black
                ]
                stat.draw(at: CGPoint(x: 50, y: yPosition), withAttributes: statAttributes)
                yPosition += 40
            }
            
            // Weekly chart (simple text representation)
            yPosition += 30
            let chartTitle = "Weekly Trend:"
            let chartAttributes: [NSAttributedString.Key: Any] = [
                .font: UIFont.boldSystemFont(ofSize: 16),
                .foregroundColor: UIColor.black
            ]
            chartTitle.draw(at: CGPoint(x: 50, y: yPosition), withAttributes: chartAttributes)
            yPosition += 30
            
            for daily in weeklyTrend {
                let dayFormatter = DateFormatter()
                dayFormatter.dateFormat = "EEE"
                let dayName = dayFormatter.string(from: daily.date)
                
                let barLength = Int(daily.totalCO2 / weeklyTrend.map { $0.totalCO2 }.max()! * 200)
                let bar = String(repeating: "█", count: barLength)
                
                let line = "\(dayName): \(bar) (\(String(format: "%.1f", daily.totalCO2)) lbs)"
                let lineAttributes: [NSAttributedString.Key: Any] = [
                    .font: UIFont.systemFont(ofSize: 12),
                    .foregroundColor: UIColor.black
                ]
                line.draw(at: CGPoint(x: 50, y: yPosition), withAttributes: lineAttributes)
                yPosition += 20
            }
        }
        
        return tempURL
    }
    
    // MARK: - Share Text (新增)
    
    func generateShareText() async throws -> String {
        let totalCO2Saved = try await repository.calculateTotalCO2Saved()
        let totalActivities = try await repository.fetchTotalActivities()
        let weeklyTrend = try await repository.fetchWeeklyTrend()
        
        let weeklyTotal = weeklyTrend.reduce(0.0) { $0 + $1.totalCO2 }
        
        var shareText = """
        🌱 My Sustainability Journey with SusLife
        
        📊 Total CO₂ Saved: \(String(format: "%.1f", totalCO2Saved)) lbs
        ✅ Activities Logged: \(totalActivities)
        📅 This Week: \(String(format: "%.1f", weeklyTotal)) lbs saved
        
        Join me in making a difference!
        """
        
        return shareText
    }
    
    // MARK: - Biometric Authentication (新增)
    
    func authenticateWithBiometrics() async -> Bool {
        let context = LAContext()
        var error: NSError?
        
        // Check if biometric authentication is available
        guard context.canEvaluatePolicy(.deviceOwnerAuthenticationWithBiometrics, error: &error) else {
            // Fallback to passcode
            return context.canEvaluatePolicy(.deviceOwnerAuthentication, error: &error)
        }
        
        do {
            let success = try await context.evaluatePolicy(
                .deviceOwnerAuthenticationWithBiometrics,
                localizedReason: "Authenticate to export your data"
            )
            return success
        } catch {
            print("Biometric authentication error: \(error)")
            return false
        }
    }
}
```

**文件：ExportView.swift (实现)**

```swift
//
//  ExportView.swift
//  suslife
//
//  Export View - Data export with biometric auth
//

import SwiftUI
import UniformTypeIdentifiers

struct ExportView: View {
    @Environment(\.dismiss) var dismiss
    @StateObject private var exportService = ExportService()
    @State private var isExporting = false
    @State private var showError = false
    @State private var errorMessage = ""
    @State private var exportedFileURL: URL?
    @State private var showShareSheet = false
    
    var body: some View {
        NavigationView {
            Form {
                Section(header: Text("Export Format")) {
                    Button(action: { exportData(format: .csv) }) {
                        HStack {
                            Image(systemName: "doc.text")
                            VStack(alignment: .leading) {
                                Text("CSV")
                                Text("Compatible with Excel, Google Sheets")
                                    .font(.caption)
                                    .foregroundColor(.secondary)
                            }
                        }
                    }
                    
                    Button(action: { exportData(format: .json) }) {
                        HStack {
                            Image(systemName: "doc.badge.gearshape")
                            VStack(alignment: .leading) {
                                Text("JSON")
                                Text("Machine-readable format")
                                    .font(.caption)
                                    .foregroundColor(.secondary)
                            }
                        }
                    }
                    
                    Button(action: { exportData(format: .pdf) }) {
                        HStack {
                            Image(systemName: "doc.richtext")
                            VStack(alignment: .leading) {
                                Text("PDF Report")
                                Text("Beautiful summary report")
                                    .font(.caption)
                                    .foregroundColor(.secondary)
                            }
                        }
                    }
                }
                
                Section(header: Text("Share")) {
                    Button(action: { shareProgress() }) {
                        HStack {
                            Image(systemName: "square.and.arrow.up")
                            Text("Share Progress")
                        }
                    }
                }
                
                if isExporting {
                    Section {
                        HStack {
                            ProgressView()
                            Text("Exporting data...")
                                .foregroundColor(.secondary)
                        }
                    }
                }
            }
            .navigationTitle("Export Data")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .navigationBarLeading) {
                    Button("Cancel") {
                        dismiss()
                    }
                }
            }
            .alert("Error", isPresented: $showError) {
                Button("OK", role: .cancel) { }
            } message: {
                Text(errorMessage)
            }
            .sheet(isPresented: $showShareSheet) {
                if let url = exportedFileURL {
                    ShareSheet(items: [url])
                }
            }
        }
    }
    
    private func exportData(format: ExportFormat) {
        Task {
            isExporting = true
            
            do {
                // Authenticate first
                let authenticated = await exportService.authenticateWithBiometrics()
                guard authenticated else {
                    await MainActor.run {
                        errorMessage = "Authentication required"
                        showError = true
                        isExporting = false
                    }
                    return
                }
                
                // Export based on format
                let fileURL: URL
                switch format {
                case .csv:
                    fileURL = try await exportService.exportToCSV()
                case .json:
                    fileURL = try await exportService.exportToJSON()
                case .pdf:
                    fileURL = try await exportService.exportToPDF()
                }
                
                await MainActor.run {
                    exportedFileURL = fileURL
                    showShareSheet = true
                    isExporting = false
                }
            } catch {
                await MainActor.run {
                    errorMessage = error.localizedDescription
                    showError = true
                    isExporting = false
                }
            }
        }
    }
    
    private func shareProgress() {
        Task {
            do {
                let shareText = try await exportService.generateShareText()
                await MainActor.run {
                    showShareSheet = true
                }
            } catch {
                await MainActor.run {
                    errorMessage = error.localizedDescription
                    showError = true
                }
            }
        }
    }
}

enum ExportFormat {
    case csv, json, pdf
}

struct ShareSheet: UIViewControllerRepresentable {
    let items: [Any]
    
    func makeUIViewController(context: Context) -> UIActivityViewController {
        let controller = UIActivityViewController(
            activityItems: items,
            applicationActivities: nil
        )
        return controller
    }
    
    func updateUIViewController(_ uiViewController: UIActivityViewController, context: Context) {}
}

#Preview {
    ExportView()
}
```

##### 2. 测试用例

```swift
//
//  ExportServiceTests.swift
//  suslifeTests
//
//  Export service tests
//

import XCTest
@testable import suslife

final class ExportServiceTests: XCTestCase {
    
    var service: ExportService!
    var mockRepository: MockActivityRepository!
    
    override func setUp() async throws {
        mockRepository = MockActivityRepository()
        service = ExportService(repository: mockRepository)
    }
    
    /// Test CSV export format
    func testCSVExport() async throws {
        // Act
        let csvURL = try await service.exportToCSV()
        
        // Assert
        let csvContent = try String(contentsOf: csvURL)
        XCTAssertTrue(csvContent.hasPrefix("Date,Category,Activity Type,Value,Unit,CO2 Saved (lbs),Notes"))
        XCTAssertTrue(csvContent.contains("car"))
        XCTAssertTrue(csvContent.contains("lbs"))
    }
    
    /// Test JSON export format
    func testJSONExport() async throws {
        // Act
        let jsonURL = try await service.exportToJSON()
        
        // Assert
        let jsonData = try Data(contentsOf: jsonURL)
        let jsonObject = try JSONSerialization.jsonObject(with: jsonData) as? [[String: Any]]
        XCTAssertNotNil(jsonObject)
        XCTAssertGreaterThan(jsonObject?.count ?? 0, 0)
    }
    
    /// Test PDF export creates file
    func testPDFExport() async throws {
        // Act
        let pdfURL = try await service.exportToPDF()
        
        // Assert
        XCTAssertTrue(FileManager.default.fileExists(atPath: pdfURL.path))
        XCTAssertEqual(pdfURL.pathExtension, "pdf")
    }
    
    /// Test share text generation
    func testShareTextGeneration() async throws {
        // Act
        let shareText = try await service.generateShareText()
        
        // Assert
        XCTAssertTrue(shareText.contains("🌱 My Sustainability Journey"))
        XCTAssertTrue(shareText.contains("Total CO₂ Saved"))
        XCTAssertTrue(shareText.contains("Activities Logged"))
    }
}
```

##### 3. 验收标准

- ✅ 支持 CSV 格式导出
- ✅ 支持 JSON 格式导出
- ✅ 支持 PDF 报告格式导出
- ✅ 导出前需生物识别验证
- ✅ 导出失败有清晰错误提示
- ✅ 导出成功后自动打开分享界面
- ✅ 分享界面支持多种分享方式
- ✅ PDF 报告美观（包含统计 + 图表）
- ✅ 大数量导出无性能问题（< 5 秒）
- ✅ 测试覆盖率 > 85%

---

### 优化 4: Profile 功能增强 ⭐⭐⭐

#### 需求描述
增强 Profile 页面，显示详细统计数据、完整成就墙、编辑功能，提升用户个人中心的价值。

#### 生成规则
- **模块位置**: 
  - `/Views/Profile/ProfileView.swift` (增强)
  - `/ViewModels/ProfileViewModel.swift` (增强)
- **继承关系**: 复用现有 AchievementService
- **UI 风格**: 保持现有设计系统（Colors, Fonts）

#### 检验标准

##### 1. 代码示例

**文件：ProfileView.swift (增强版)**

```swift
//
//  ProfileView.swift
//  suslife
//
//  Profile View - Enhanced with detailed stats and achievements
//

import SwiftUI

struct ProfileView: View {
    @StateObject private var viewModel = ProfileViewModel()
    @State private var showAchievementList = false
    @State private var showEditProfile = false
    
    var body: some View {
        ScrollView {
            VStack(spacing: 20) {
                ProfileHeader(
                    stats: viewModel.userStats,
                    onEdit: { showEditProfile = true }
                )
                
                DetailedStatsCard(stats: viewModel.userStats)
                
                AchievementWall(
                    achievements: viewModel.achievements,
                    progress: viewModel.achievementProgress,
                    onTapAction: { showAchievementList = true }
                )
                
                ImpactSummary(stats: viewModel.userStats)
            }
            .padding()
        }
        .navigationTitle("Profile")
        .task {
            await viewModel.loadData()
        }
        .sheet(isPresented: $showAchievementList) {
            AchievementListView(
                achievementService: AchievementService(repository: CoreDataActivityRepository())
            )
        }
        .sheet(isPresented: $showEditProfile) {
            EditProfileView(stats: viewModel.userStats)
        }
    }
}

// MARK: - Profile Header

struct ProfileHeader: View {
    let stats: UserStats
    let onEdit: () -> Void
    
    var body: some View {
        VStack(spacing: 12) {
            HStack {
                Spacer()
                
                Button(action: onEdit) {
                    Image(systemName: "pencil.circle.fill")
                        .font(.system(size: 24))
                        .foregroundColor(AppColors.primary)
                }
            }
            
            Circle()
                .fill(AppColors.primary.opacity(0.2))
                .frame(width: 80, height: 80)
                .overlay(
                    Image(systemName: "leaf.fill")
                        .font(.system(size: 40))
                        .foregroundColor(AppColors.primary)
                )
            
            Text("Eco Warrior")
                .font(Fonts.title2)
                .fontWeight(.semibold)
            
            Text("Member since \(joinDateFormatted)")
                .font(Fonts.footnote)
                .foregroundColor(AppColors.textSecondary)
        }
        .padding()
        .background(AppColors.cardBackground)
        .cornerRadius(16)
    }
    
    private var joinDateFormatted: String {
        let formatter = DateFormatter()
        formatter.dateFormat = "MMM yyyy"
        return formatter.string(from: stats.joinDate)
    }
}

// MARK: - Detailed Stats Card (新增)

struct DetailedStatsCard: View {
    let stats: UserStats
    
    var body: some View {
        VStack(alignment: .leading, spacing: 16) {
            Text("Detailed Statistics")
                .font(Fonts.title3)
                .fontWeight(.semibold)
            
            LazyVGrid(columns: [GridItem(.flexible()), GridItem(.flexible())], spacing: 16) {
                StatDetailItem(
                    icon: "leaf.fill",
                    label: "Total CO₂ Saved",
                    value: String(format: "%.0f", stats.totalCO2Saved),
                    unit: "lbs",
                    color: AppColors.primary
                )
                
                StatDetailItem(
                    icon: "checkmark.circle.fill",
                    label: "Activities Logged",
                    value: String(stats.totalActivities),
                    unit: "total",
                    color: AppColors.accent
                )
                
                StatDetailItem(
                    icon: "flame.fill",
                    label: "Current Streak",
                    value: String(stats.currentStreak),
                    unit: "days",
                    color: AppColors.warning
                )
                
                StatDetailItem(
                    icon: "calendar",
                    label: "Days Active",
                    value: String(calculateDaysActive()),
                    unit: "days",
                    color: AppColors.success
                )
            }
        }
        .padding()
        .background(AppColors.cardBackground)
        .cornerRadius(16)
    }
    
    private func calculateDaysActive() -> Int {
        let calendar = Calendar.current
        let components = calendar.dateComponents(
            [.day],
            from: stats.joinDate,
            to: Date()
        )
        return components.day ?? 0
    }
}

struct StatDetailItem: View {
    let icon: String
    let label: String
    let value: String
    let unit: String
    let color: Color
    
    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            HStack(spacing: 8) {
                Image(systemName: icon)
                    .font(.system(size: 20))
                    .foregroundColor(color)
                
                Text(label)
                    .font(Fonts.caption1)
                    .foregroundColor(AppColors.textSecondary)
            }
            
            HStack(alignment: .firstTextBaseline, spacing: 4) {
                Text(value)
                    .font(Fonts.title2)
                    .fontWeight(.bold)
                    .foregroundColor(AppColors.textPrimary)
                
                Text(unit)
                    .font(Fonts.caption1)
                    .foregroundColor(AppColors.textSecondary)
            }
        }
        .frame(maxWidth: .infinity, alignment: .leading)
        .padding()
        .background(color.opacity(0.1))
        .cornerRadius(12)
    }
}

// MARK: - Impact Summary (新增)

struct ImpactSummary: View {
    let stats: UserStats
    
    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Your Environmental Impact")
                .font(Fonts.title3)
                .fontWeight(.semibold)
            
            VStack(alignment: .leading, spacing: 8) {
                ImpactRow(
                    icon: "tree.fill",
                    description: "Equivalent to planting",
                    value: String(format: "%.0f", stats.totalCO2Saved / 48),
                    unit: "trees"
                )
                
                ImpactRow(
                    icon: "car.side.fill",
                    description: "Equivalent to removing",
                    value: String(format: "%.0f", stats.totalCO2Saved / 8800),
                    unit: "cars for a year"
                )
                
                ImpactRow(
                    icon: "bolt.fill",
                    description: "Equivalent to saving",
                    value: String(format: "%.0f", stats.totalCO2Saved * 0.453),
                    unit: "kWh of energy"
                )
            }
        }
        .padding()
        .background(AppColors.cardBackground)
        .cornerRadius(16)
    }
}

struct ImpactRow: View {
    let icon: String
    let description: String
    let value: String
    let unit: String
    
    var body: some View {
        HStack(spacing: 12) {
            Image(systemName: icon)
                .font(.system(size: 20))
                .foregroundColor(AppColors.accent)
                .frame(width: 30)
            
            Text("\(description) **\(value) \(unit)**")
                .font(Fonts.body)
                .foregroundColor(AppColors.textSecondary)
            
            Spacer()
        }
    }
}

// MARK: - Preview

struct ProfileView_Previews: PreviewProvider {
    static var previews: some View {
        ProfileView()
    }
}
```

##### 2. 验收标准

- ✅ Profile 显示详细统计数据（4 项）
- ✅ 新增环境影响换算（树木、汽车、能源）
- ✅ 成就墙显示完整进度条
- ✅ 支持编辑个人资料
- ✅ 计算活跃天数
- ✅ UI 风格与现有设计一致
- ✅ 数据加载流畅（< 1 秒）

---

### 优化 5: 代码重构与清理 ⭐⭐⭐

#### 需求描述
清理废弃代码，合并重复逻辑，提升代码质量和可维护性。

#### 生成规则
- **清理范围**: 标记废弃 → 验证无影响 → 删除
- **复用原则**: 三次法则（出现 3 次再抽象）
- **提交信息**: 注明清理范围和影响

#### 检验标准

##### 1. 清理清单

**待清理项**:

```markdown
## 废弃代码清理清单

### 1. DashboardViewModel.loadAchievements() - TODO 占位符
- **位置**: `ViewModels/DashboardViewModel.swift:86-90`
- **现状**: 空实现，返回空数组
- **处理**: 集成 AchievementService
- **影响**: 无（已被 AchievementService 替代）

### 2. ProfileViewModel.achievementService - 未使用
- **位置**: `ViewModels/ProfileViewModel.swift:18`
- **现状**: Optional，未初始化
- **处理**: 移除 optional，直接注入
- **影响**: 无

### 3. NotificationService.checkActiveStreak() - TODO
- **位置**: `Services/NotificationService.swift:158-162`
- **现状**: 返回 false
- **处理**: 集成 Repository
- **影响**: 低（streak 提醒未启用）

### 4. AchievementService.checkRequirement() - 重复逻辑
- **位置**: `Services/AchievementService.swift:68-85`
- **现状**: switch-case 重复检查
- **处理**: 提取为策略模式
- **影响**: 中（需要测试验证）

### 5. 重复的日期格式化代码
- **位置**: 多个 View 文件
- **现状**: 每个 View 自己创建 DateFormatter
- **处理**: 提取为扩展 `DateFormatter.suslife`
- **影响**: 低（性能优化）
```

**清理步骤**:

```swift
// 步骤 1: 标记废弃（@available 属性）
@available(*, deprecated, message: "Use AchievementService instead")
private func loadAchievements() async {
    // 旧代码
}

// 步骤 2: 验证无影响（运行所有测试）
xcodebuild test -scheme suslife

// 步骤 3: 删除废弃代码
// 删除 loadAchievements() 方法

// 步骤 4: 提交
git commit -m "refactor: Remove deprecated loadAchievements method

- Removed unused DashboardViewModel.loadAchievements()
- AchievementService now handles all achievement logic
- Tests updated to use AchievementService
- No breaking changes to public API"
```

##### 2. 代码复用优化

**提取公共 DateFormatter**:

```swift
// 新增：Resources/DateFormatters.swift
//
//  DateFormatters.swift
//  suslife
//
//  Shared date formatters for consistent formatting
//

import Foundation

extension DateFormatter {
    // MARK: - Shared Formatters
    
    static let suslifeDisplay: DateFormatter = {
        let formatter = DateFormatter()
        formatter.dateFormat = "MMM d, yyyy"
        return formatter
    }()
    
    static let suslifeDateTime: DateFormatter = {
        let formatter = DateFormatter()
        formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        return formatter
    }()
    
    static let suslifeISO8601: DateFormatter = {
        let formatter = DateFormatter()
        formatter.dateFormat = "yyyy-MM-dd'T'HH:mm:ssZ"
        formatter.timeZone = TimeZone(secondsFromGMT: 0)
        return formatter
    }()
    
    static let suslifeDayOfWeek: DateFormatter = {
        let formatter = DateFormatter()
        formatter.dateFormat = "EEE"
        return formatter
    }()
    
    static let suslifeMonthYear: DateFormatter = {
        let formatter = DateFormatter()
        formatter.dateFormat = "MMM yyyy"
        return formatter
    }()
}

// 使用示例
let dateStr = Date().formatted(.suslifeDisplay)
// 替代：let formatter = DateFormatter(); formatter.dateFormat = "..."
```

**提取公共计算逻辑**:

```swift
// 新增：Extensions/Date+Extensions.swift
//
//  Date+Extensions.swift
//  suslife
//
//  Date helper extensions
//

import Foundation

extension Date {
    /// Start of day (midnight)
    var startOfDay: Date {
        Calendar.current.startOfDay(for: self)
    }
    
    /// End of day (23:59:59)
    var endOfDay: Date {
        var components = DateComponents()
        components.day = 1
        components.second = -1
        return Calendar.current.date(byAdding: components, to: startOfDay)!
    }
    
    /// Days since another date
    func daysSince(_ earlierDate: Date) -> Int {
        Calendar.current.dateComponents([.day], from: earlierDate, to: self).day ?? 0
    }
    
    /// Is today?
    var isToday: Bool {
        Calendar.current.isDateInToday(self)
    }
    
    /// Is yesterday?
    var isYesterday: Bool {
        Calendar.current.isDateInYesterday(self)
    }
}
```

##### 3. 验收标准

- ✅ 删除所有 TODO 占位符
- ✅ 删除所有@available deprecated 代码
- ✅ 提取公共 DateFormatter（减少重复）
- ✅ 提取 Date 扩展（复用计算逻辑）
- ✅ 所有测试通过
- ✅ 代码行数减少 > 10%
- ✅ 无功能退化

---

## 📅 实施计划

### 阶段 1: 成就系统完善 (3-4 天)
- **Day 1**: AchievementService 增强 + 通知集成
- **Day 2**: AchievementUnlockPopup 实现
- **Day 3**: DashboardView 集成 + 动画调试
- **Day 4**: 测试 + Bug 修复

### 阶段 2: 通知系统完善 (2-3 天)
- **Day 1**: NotificationService 增强
- **Day 2**: Settings 集成 + 配置界面
- **Day 3**: 测试 + 优化

### 阶段 3: 数据导出完善 (2-3 天)
- **Day 1**: ExportService PDF 功能
- **Day 2**: ExportView 实现 + 生物识别
- **Day 3**: 测试 + 优化

### 阶段 4: Profile 增强 (2 天)
- **Day 1**: ProfileView 详细统计
- **Day 2**: ImpactSummary + 测试

### 阶段 5: 代码重构清理 (2 天)
- **Day 1**: 标记废弃代码 + 提取公共组件
- **Day 2**: 删除废弃代码 + 提交

**总工期**: 11-14 天

---

## ✅ 验收标准总览

| 优化项 | 完成度 | 测试覆盖 | 文档 | 代码审查 |
|--------|--------|----------|------|----------|
| 成就系统 | 100% | >90% | ✅ | ✅ |
| 通知系统 | 100% | >85% | ✅ | ✅ |
| 数据导出 | 100% | >85% | ✅ | ✅ |
| Profile 增强 | 100% | >80% | ✅ | ✅ |
| 代码清理 | 100% | N/A | ✅ | ✅ |

---

## 📝 提交规范

### Git Commit Message 格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

**示例**:

```
feat(achievement): Add achievement unlock popup animation

- Implement AchievementUnlockPopup view with spring animation
- Integrate NotificationService for achievement notifications
- Add haptic feedback on unlock
- Auto-hide popup after 3 seconds

Closes #123
```

**类型说明**:
- `feat`: 新功能
- `fix`: Bug 修复
- `refactor`: 代码重构
- `test`: 测试相关
- `docs`: 文档更新
- `chore`: 构建/工具

---

## 🎯 成功指标

### 用户体验指标
- ✅ 成就解锁动画流畅（60 FPS）
- ✅ 通知点击率 > 30%
- ✅ 导出功能使用率 > 20%
- ✅ Profile 页面停留时间 > 30 秒

### 代码质量指标
- ✅ 测试覆盖率 > 85%
- ✅ 代码重复率 < 5%
- ✅ 无 TODO 占位符
- ✅ 所有函数 < 50 行

### 性能指标
- ✅ 应用启动时间 < 2 秒
- ✅ 页面切换 < 0.3 秒
- ✅ 数据导出 < 5 秒
- ✅ 内存占用 < 100MB

---

## 📚 附录

### A. 相关文件清单
- `/Services/AchievementService.swift`
- `/Services/NotificationService.swift`
- `/Services/ExportService.swift`
- `/Views/Achievement/AchievementUnlockPopup.swift`
- `/Views/Profile/ProfileView.swift`
- `/Views/Settings/ExportView.swift`
- `/Resources/DateFormatters.swift`
- `/Extensions/Date+Extensions.swift`

### B. 测试文件清单
- `/suslifeTests/AchievementServiceTests.swift`
- `/suslifeTests/NotificationServiceTests.swift`
- `/suslifeTests/ExportServiceTests.swift`
- `/suslifeTests/ProfileViewModelTests.swift`

### C. 依赖关系图
```
DashboardView
  ├─ DashboardViewModel
  │   └─ ActivityRepositoryProtocol
  └─ AchievementService
      ├─ ActivityRepositoryProtocol
      └─ NotificationService

ProfileView
  ├─ ProfileViewModel
  │   ├─ ActivityRepositoryProtocol
  │   └─ UserRepositoryProtocol
  └─ AchievementService

SettingsView
  ├─ NotificationService
  └─ ExportService
      └─ ActivityRepositoryProtocol
```

---

**文档版本**: 2.0  
**最后更新**: 2026-03-11  
**审核状态**: 待审核  
**批准人**: [待填写]
