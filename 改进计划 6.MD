# 改进计划 6.MD - 核心功能修复与优化

**创建日期**: 2026-03-13  
**优先级**: 🔴 高  
**预计投入**: 2-3 天  
**状态**: 待实施  
**版本**: 6.0

---

## 📋 目录

1. [问题背景](#问题背景)
2. [优化方案总览](#优化方案总览)
3. [详细实施方案](#详细实施方案)
4. [测试计划](#测试计划)
5. [验收标准](#验收标准)
6. [实施时间表](#实施时间表)

---

## 问题背景

### **核心问题识别**

经过深度代码审查，发现以下高优先级问题：

#### **问题 1: Streak 计算逻辑错误** 🔴

**当前实现**（CommunityView.swift:320）：
```swift
// ❌ 错误：只是简单地将周数乘以 7
let profile = try await userProfileRepository.getUserProfile()
currentStreak = Int(profile.weeklyStreak) * 7  // 不是真正的连续天数
```

**问题影响**:
- 用户看到的"连续记录天数"不准确
- 如果用户某天没有记录，streak 不会正确中断
- 影响用户成就解锁（如"7天连续记录"成就）

**正确逻辑**:
- 从最近一天往回数，检查每天都有活动记录
- 遇到某天没有记录，streak 中断
- 返回实际连续天数

---

#### **问题 2: 成就触发时机不正确** 🔴

**当前实现**（CommunityView.swift:52）：
```swift
// ❌ 问题：只在页面加载时检查成就
.task {
    await loadData()
    await achievementService.checkAchievements()  // 触发时机太晚
}
```

**问题影响**:
- 用户记录活动后，成就不会立即解锁
- 需要退出并重新进入 My Progress 页面才能看到新成就
- 用户体验差，缺乏即时反馈

**正确逻辑**:
- 在活动保存成功后立即检查成就
- 如果有新解锁的成就，立即显示通知
- 实时更新成就状态

---

#### **问题 3: 错误处理不完善** 🟡

**当前实现**（CommunityView.swift:322）：
```swift
// ❌ 问题：只打印错误，没有给用户反馈
do {
    // ... 加载数据
} catch {
    print("Error loading progress data: \(error)")  // 用户看不到
}
```

**问题影响**:
- 数据加载失败时，用户看到空白页面或旧数据
- 不知道发生了什么，无法采取纠正措施
- 体验差，可能导致用户流失

**正确逻辑**:
- 显示用户友好的错误提示
- 提供重试机制
- 显示加载状态

---

## 优化方案总览

### **优化原则**

1. ✅ **复用优先** - 复用现有 Repository 和 Service
2. ✅ **单一职责** - 每个功能一个模块
3. ✅ **最小改动** - 保持现有架构，只修复问题
4. ✅ **向后兼容** - 不影响现有功能
5. ✅ **美国本地化** - 全英文 UI，符合美国使用习惯

### **优化内容**

| 序号 | 优化项 | 优先级 | 文件 | 说明 |
|------|--------|--------|------|------|
| 1 | Streak 计算修复 | 🔴 高 | ActivityRepositoryProtocol + CoreDataActivityRepository | 实现真正的连续天数计算 |
| 2 | 成就触发修复 | 🔴 高 | LogActivityViewModel + AchievementService | 活动保存后立即检查成就 |
| 3 | 错误处理完善 | 🟡 中 | CommunityView + DashboardView | 添加错误提示和重试机制 |
| 4 | 成就通知 UI | 🟡 中 | AchievementUnlockPopup（复用） | 显示成就解锁通知 |

---

## 详细实施方案

### **优化 1: Streak 计算修复** 🔴

#### **需求描述**

实现真正的连续记录天数计算，从最近一天往回数，检查每天都有活动记录，遇到某天没有记录则中断。

#### **生成规则**

**1. 在 ActivityRepositoryProtocol 中添加新方法**

```swift
// ActivityRepositoryProtocol.swift

/// Calculate current streak (consecutive days with activities)
/// Returns the number of consecutive days from today backwards
/// where the user logged at least one activity each day
func calculateCurrentStreak() async throws -> Int
```

**2. 在 CoreDataActivityRepository 中实现**

```swift
// CoreDataActivityRepository.swift

func calculateCurrentStreak() async throws -> Int {
    let context = coreDataStack.mainContext
    
    return try await context.perform {
        let calendar = Calendar.current
        var streak = 0
        var currentDate = Date()
        
        // Check up to 365 days back (reasonable limit)
        for _ in 0..<365 {
            let startOfDay = calendar.startOfDay(for: currentDate)
            let endOfDay = calendar.date(byAdding: .day, value: 1, to: startOfDay)!
            
            let request: NSFetchRequest<CarbonActivity> = CarbonActivity.fetchRequest()
            request.predicate = NSPredicate(
                format: "date >= %@ AND date < %@",
                startOfDay as NSDate,
                endOfDay as NSDate
            )
            request.fetchLimit = 1  // Only need to know if any exist
            
            let count = try context.count(for: request)
            
            if count > 0 {
                streak += 1
                // Move to previous day
                currentDate = calendar.date(byAdding: .day, value: -1, to: currentDate)!
            } else {
                // Streak broken
                break
            }
        }
        
        return streak
    }
}
```

**3. 更新 CommunityView 使用新方法**

```swift
// CommunityView.swift

// 修改前：
// currentStreak = Int(profile.weeklyStreak) * 7

// 修改后：
currentStreak = try await repository.calculateCurrentStreak()
```

#### **检验标准**

**代码编写示例**:
```swift
// 测试场景 1: 用户连续 5 天有记录
// Day -4: 有活动
// Day -3: 有活动
// Day -2: 有活动
// Day -1: 有活动
// Day 0 (今天): 有活动
// 预期结果: streak = 5

// 测试场景 2: 用户昨天没有记录
// Day -2: 有活动
// Day -1: 无活动
// Day 0 (今天): 有活动
// 预期结果: streak = 1（只有今天）

// 测试场景 3: 用户没有任何记录
// 预期结果: streak = 0
```

**测试用例**:
```swift
// CoreDataActivityRepositoryTests.swift

func testCalculateCurrentStreak_WithConsecutiveDays() async throws {
    // Given: 连续 3 天有活动
    let calendar = Calendar.current
    for dayOffset in 0..<3 {
        let date = calendar.date(byAdding: .day, value: -dayOffset, to: Date())!
        let activity = CarbonActivity(context: coreDataStack.mainContext)
        activity.id = UUID()
        activity.date = date
        activity.co2Emission = 1.0
    }
    try coreDataStack.mainContext.save()
    
    // When
    let streak = try await repository.calculateCurrentStreak()
    
    // Then
    XCTAssertEqual(streak, 3)
}

func testCalculateCurrentStreak_WithGap() async throws {
    // Given: 今天和前天有活动，昨天没有
    let calendar = Calendar.current
    
    // 今天
    let today = CarbonActivity(context: coreDataStack.mainContext)
    today.date = Date()
    today.co2Emission = 1.0
    
    // 前天（跳过昨天）
    let twoDaysAgo = calendar.date(byAdding: .day, value: -2, to: Date())!
    let activity = CarbonActivity(context: coreDataStack.mainContext)
    activity.date = twoDaysAgo
    activity.co2Emission = 1.0
    
    try coreDataStack.mainContext.save()
    
    // When
    let streak = try await repository.calculateCurrentStreak()
    
    // Then: streak 应该只有 1（只有今天）
    XCTAssertEqual(streak, 1)
}

func testCalculateCurrentStreak_NoActivities() async throws {
    // Given: 没有任何活动
    
    // When
    let streak = try await repository.calculateCurrentStreak()
    
    // Then
    XCTAssertEqual(streak, 0)
}
```

**注释要求**:
```swift
/// Calculates the current streak of consecutive days with logged activities.
/// 
/// This method counts backwards from today, checking each day for at least one activity.
/// The streak breaks when a day with no activities is encountered.
/// 
/// - Returns: The number of consecutive days (including today) with at least one activity
/// - Throws: CoreData errors if fetch fails
/// 
/// Example:
/// - User logged activities today, yesterday, and 2 days ago: returns 3
/// - User logged activities today and 2 days ago (skipped yesterday): returns 1
/// - User has no activities: returns 0
func calculateCurrentStreak() async throws -> Int
```

---

### **优化 2: 成就触发修复** 🔴

#### **需求描述**

在活动保存成功后立即检查成就，如果有新解锁的成就，立即显示通知。

#### **生成规则**

**1. 修改 LogActivityViewModel.saveActivity**

```swift
// LogActivityViewModel.swift

// 添加 AchievementService 依赖
private let achievementService: AchievementService

init(
    repository: ActivityRepositoryProtocol = CoreDataActivityRepository(),
    achievementService: AchievementService = AchievementService()  // 新增
) {
    self.repository = repository
    self.achievementService = achievementService
}

func saveActivity(...) async throws -> Double {
    let input = ActivityInput(...)
    
    // 验证...
    
    do {
        let activity = try await repository.save(input)
        
        // 通知事件
        ActivityEvent.notifyActivitySaved(co2Amount: activity.co2Emission, category: category)
        
        // ✅ 新增：立即检查成就
        await achievementService.checkAchievements()
        
        // ✅ 新增：如果有新解锁的成就，显示通知
        if !achievementService.recentlyUnlocked.isEmpty {
            await MainActor.run {
                // 触发通知（通过 Published 属性自动触发 UI 更新）
                showSuccess = true
            }
        }
        
        return activity.co2Emission
    } catch {
        // 错误处理...
    }
}
```

**2. 修改 LogActivityView 监听成就通知**

```swift
// LogActivityView.swift

// 添加状态
@State private var showAchievementUnlock = false
@State private var unlockedAchievement: Achievement?

// 在 body 中添加弹窗
.sheet(isPresented: $showAchievementUnlock) {
    if let achievement = unlockedAchievement {
        AchievementUnlockPopup(achievement: achievement)
    }
}

// 在 saveActivity 成功后检查成就
private func saveActivity() {
    // ... 保存逻辑 ...
    
    Task {
        do {
            let co2 = try await activityViewModel.saveActivity(...)
            
            // ✅ 检查是否有新解锁的成就
            if let achievement = activityViewModel.achievementService.recentlyUnlocked.first {
                await MainActor.run {
                    unlockedAchievement = achievement
                    showAchievementUnlock = true
                }
            }
            
            // 关闭页面
            await MainActor.run {
                dismiss()
            }
            
            // 刷新 Dashboard
            await viewModel.loadData()
        } catch {
            // 错误处理...
        }
    }
}
```

**3. 复用现有的 AchievementUnlockPopup**

```swift
// AchievementUnlockPopup.swift（已存在，复用）

struct AchievementUnlockPopup: View {
    let achievement: Achievement
    @Environment(\.dismiss) var dismiss
    
    var body: some View {
        VStack(spacing: 24) {
            // 成就图标
            Image(systemName: achievement.iconName)
                .font(.system(size: 80))
                .foregroundColor(.yellow)
            
            // 标题
            Text("Achievement Unlocked!")
                .font(.title)
                .fontWeight(.bold)
            
            // 成就名称
            Text(achievement.title)
                .font(.title2)
                .foregroundColor(AppColors.primary)
            
            // 描述
            Text(achievement.description)
                .font(.body)
                .foregroundColor(.secondary)
                .multilineTextAlignment(.center)
            
            // XP 奖励
            Text("+\(achievement.xpReward) XP")
                .font(.headline)
                .foregroundColor(.orange)
            
            // 关闭按钮
            Button("Awesome!") {
                dismiss()
            }
            .buttonStyle(.borderedProminent)
            .tint(AppColors.primary)
            .padding(.top)
        }
        .padding(32)
        .background(AppColors.cardBackground)
        .cornerRadius(20)
        .padding()
    }
}
```

#### **检验标准**

**代码编写示例**:
```swift
// 测试场景：用户记录第一个活动
// 预期：触发 "First Step" 成就，显示弹窗

// 测试场景：用户累计记录 7 天
// 预期：触发 "Week Warrior" 成就，显示弹窗

// 测试场景：用户记录活动但没有新成就
// 预期：不显示弹窗，正常关闭页面
```

**测试用例**:
```swift
// LogActivityViewModelTests.swift

func testSaveActivity_TriggersAchievementCheck() async throws {
    // Given
    let mockRepository = MockActivityRepository()
    let mockAchievementService = MockAchievementService()
    let viewModel = LogActivityViewModel(
        repository: mockRepository,
        achievementService: mockAchievementService
    )
    
    // When
    _ = try await viewModel.saveActivity(
        category: "transport",
        activityType: "walking",
        value: 1.0,
        unit: "mi",
        notes: nil
    )
    
    // Then: 应该调用成就检查
    XCTAssertTrue(mockAchievementService.checkAchievementsCalled)
}

func testSaveActivity_ShowsUnlockPopup() async throws {
    // Given: 有一个即将解锁的成就
    let mockAchievementService = MockAchievementService()
    mockAchievementService.mockRecentlyUnlocked = [
        Achievement(id: "first_step", title: "First Step", ...)
    ]
    
    let viewModel = LogActivityViewModel(
        achievementService: mockAchievementService
    )
    
    // When
    _ = try await viewModel.saveActivity(...)
    
    // Then: 应该标记显示成功（触发弹窗）
    XCTAssertTrue(viewModel.showSuccess)
}
```

---

### **优化 3: 错误处理完善** 🟡

#### **需求描述**

为 CommunityView 和 DashboardView 添加完善的错误处理和用户反馈机制。

#### **生成规则**

**1. 修改 CommunityView**

```swift
// CommunityView.swift

// 添加状态
@State private var showErrorAlert = false
@State private var errorMessage = ""
@State private var showRetryButton = false

// 在 body 中添加 alert
.alert("Error", isPresented: $showErrorAlert) {
    if showRetryButton {
        Button("Retry") {
            Task {
                await loadData()
            }
        }
    }
    Button("OK", role: .cancel) { }
} message: {
    Text(errorMessage)
}

// 修改 loadData 方法
private func loadData() async {
    isLoading = true
    showRetryButton = false
    
    do {
        try await rankingService.loadCurrentWeekPerformance()
        try await rankingService.loadPerformanceComparison()
        
        totalCO2Saved = try await repository.calculateTotalCO2Saved()
        totalActivities = try await repository.fetchTotalActivities()
        currentStreak = try await repository.calculateCurrentStreak()  // 使用新方法
        
        // 清除错误状态
        errorMessage = ""
        showRetryButton = false
    } catch {
        // ✅ 用户友好的错误提示
        errorMessage = "Failed to load your progress. Please check your connection and try again."
        showErrorAlert = true
        showRetryButton = true
        print("Error loading progress data: \(error)")
    }
    
    isLoading = false
}
```

**2. 修改 DashboardView（类似逻辑）**

```swift
// DashboardView.swift

// 添加错误状态
@State private var showErrorAlert = false
@State private var errorMessage = ""

// 在 body 中添加 alert
.alert("Error Loading Data", isPresented: $showErrorAlert) {
    Button("Retry") {
        Task {
            await viewModel.loadData()
        }
    }
    Button("OK", role: .cancel) { }
} message: {
    Text(errorMessage)
}

// 在 viewModel.loadData() 失败时显示
.task {
    await viewModel.loadData()
    if let error = viewModel.errorMessage {
        errorMessage = error
        showErrorAlert = true
    }
}
```

#### **检验标准**

**测试用例**:
```swift
// CommunityViewTests.swift（UI 测试）

func testLoadDataFailure_ShowsErrorAlert() {
    // Given: 模拟网络/数据库错误
    let app = XCUIApplication()
    app.launch()
    
    // When: 进入 My Progress 页面
    app.tabBars["My Progress"].tap()
    
    // Then: 应该显示错误提示
    XCTAssertTrue(app.alerts["Error"].exists)
    XCTAssertTrue(app.buttons["Retry"].exists)
}
```

---

## 测试计划

### **单元测试**

| 测试文件 | 测试内容 | 覆盖率目标 |
|----------|----------|------------|
| CoreDataActivityRepositoryTests.swift | calculateCurrentStreak | 100% |
| LogActivityViewModelTests.swift | 成就触发逻辑 | 100% |
| AchievementServiceTests.swift | 成就检查流程 | 100% |

### **UI 测试**

| 测试场景 | 预期结果 |
|----------|----------|
| 记录活动后解锁成就 | 显示成就弹窗 |
| 数据加载失败 | 显示错误提示和重试按钮 |
| 连续多天记录 | Streak 正确计算 |

### **手动测试清单**

- [ ] Streak 计算准确（连续天数）
- [ ] 成就立即解锁（记录活动后）
- [ ] 成就弹窗显示正常
- [ ] 错误提示友好
- [ ] 重试功能正常
- [ ] 无崩溃

---

## 验收标准

### **功能验收**

#### ✅ Streak 计算
- [ ] 连续 5 天记录 → streak = 5
- [ ] 中间跳过 1 天 → streak 中断
- [ ] 无记录 → streak = 0
- [ ] 性能 < 100ms（365 天检查）

#### ✅ 成就触发
- [ ] 记录活动后立即检查成就
- [ ] 新成就解锁时显示弹窗
- [ ] 弹窗显示成就名称、描述、XP
- [ ] 点击按钮关闭弹窗

#### ✅ 错误处理
- [ ] 数据加载失败显示友好提示
- [ ] 提供重试按钮
- [ ] 错误信息清晰（英文）

### **代码质量**

- [ ] 无编译错误/警告
- [ ] 单元测试通过率 100%
- [ ] 代码注释完整
- [ ] 符合 Swift 规范

### **兼容性**

- [ ] 不影响现有功能
- [ ] 向后兼容（旧数据正常）
- [ ] iOS 16+ 支持

---

## 实施时间表

### **Day 1: Streak 计算修复**
- [ ] 添加 calculateCurrentStreak() 到 Protocol
- [ ] 实现 CoreDataActivityRepository.calculateCurrentStreak()
- [ ] 编写单元测试
- [ ] 更新 CommunityView 使用新方法
- [ ] 手动测试

### **Day 2: 成就触发修复**
- [ ] 修改 LogActivityViewModel 添加 AchievementService
- [ ] 修改 saveActivity 触发成就检查
- [ ] 修改 LogActivityView 显示成就弹窗
- [ ] 编写单元测试
- [ ] 手动测试

### **Day 3: 错误处理完善**
- [ ] 修改 CommunityView 添加错误处理
- [ ] 修改 DashboardView 添加错误处理
- [ ] 编写 UI 测试
- [ ] 全面测试
- [ ] Git 提交

---

## 代码清理计划

### **废弃代码标记**

```swift
// CommunityView.swift

// ❌ 废弃：旧的 streak 计算方式
// currentStreak = Int(profile.weeklyStreak) * 7
// 原因：不是真正的连续天数计算
// 替换：使用 repository.calculateCurrentStreak()
```

### **Git 提交策略**

```bash
# 提交 1: Streak 计算修复
git add suslife/Repositories/Protocols/ActivityRepositoryProtocol.swift
git add suslife/Repositories/CoreDataActivityRepository.swift
git add suslife/Views/Community/CommunityView.swift
git add suslifeTests/CoreDataActivityRepositoryTests.swift
git commit -m "fix: Implement accurate streak calculation

- Add calculateCurrentStreak() to ActivityRepositoryProtocol
- Implement true consecutive days calculation in CoreDataActivityRepository
- Update CommunityView to use new method
- Add comprehensive unit tests

Fixes issue where streak was incorrectly calculated as weeklyStreak * 7"

# 提交 2: 成就触发修复
git add suslife/ViewModels/LogActivityViewModel.swift
git add suslife/Views/LogActivity/LogActivityView.swift
git add suslifeTests/LogActivityViewModelTests.swift
git commit -m "feat: Trigger achievement check immediately after activity save

- Modify LogActivityViewModel to check achievements after save
- Show AchievementUnlockPopup when new achievements unlocked
- Improve user experience with instant feedback

Fixes issue where achievements only checked on page load"

# 提交 3: 错误处理完善
git add suslife/Views/Community/CommunityView.swift
git add suslife/Views/Dashboard/DashboardView.swift
git commit -m "feat: Add comprehensive error handling with user feedback

- Add error alerts with retry buttons
- Show user-friendly error messages
- Improve loading states

Enhances UX when data loading fails"
```

---

## 风险评估

### **风险 1: 性能问题（Streak 计算）**
**概率**: 低  
**影响**: 中  
**缓解**: 使用 fetchLimit = 1，最多检查 365 天

### **风险 2: 成就弹窗干扰**
**概率**: 中  
**影响**: 低  
**缓解**: 只显示一次，用户可立即关闭

### **风险 3: 向后兼容性**
**概率**: 低  
**影响**: 高  
**缓解**: 充分测试，保留旧数据格式

---

## 成功指标

### **量化指标**
- Streak 计算准确率: 100%
- 成就触发延迟: < 1 秒
- 错误提示覆盖率: 100%
- 测试通过率: 100%

### **质化指标**
- 用户反馈积极
- 无 streak 计算投诉
- 成就解锁体验流畅
- 错误处理友好

---

**文档状态**: ✅ 完成  
**审核状态**: 待审核  
**实施状态**: 待实施  
**最后更新**: 2026-03-13  
**版本**: 6.0
